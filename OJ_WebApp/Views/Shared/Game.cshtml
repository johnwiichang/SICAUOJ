@using PagedList.Mvc;
@using PagedList;
@model IPagedList<OJ_WebApp.Models.Game>

@{
    ViewBag.Title = Resources.Language.CompetitionList;
    ViewBag.SmallBtn = "<a href='/Admin/AddGame' class='btn btn-outline btn-primary btn-xs' type='button'>" + Resources.Language.Add + "</a>";
}
<div class="col-lg-12">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>@Resources.Language.Name</th>
                <th>@Resources.Language.StartTime</th>
                <th>@Resources.Language.EndTime</th>
                <th>@Resources.Language.Operation</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td><a href="@Url.Action("GameDetail", "Admin", new { Id = item.Id })">@item.Name</a></td>
                    <td>@item.BeginTime.ToString()</td>
                    <th>@item.EndTime.ToString()</th>
                    <td>
                        <a href="@Url.Action("EditGame", "Admin", new { @Id = item.Id })" class="btn btn-outline btn-success btn-xs" type="button">@Resources.Language.Edit</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @Html.PagedListPager((IPagedList)Model, page => Url.Action("Game", "Admin", new { page }))
</div>
